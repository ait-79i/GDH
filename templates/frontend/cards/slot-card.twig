<div class="gdhrdv-rdv-slot-card" data-index="{{ index }}">
	<div class="gdhrdv-rdv-slot-header">
		<div class="gdhrdv-rdv-slot-badge">
			<span class="dashicons dashicons-calendar-alt"></span>
			<span class="gdhrdv-rdv-slot-number">#{{ index }}</span>
		</div>
		<h5 class="gdhrdv-rdv-slot-title">{{ cardTitle }}</h5>
		{% if showRemove %}
			<button type="button" class="gdhrdv-rdv-remove-slot" title="Supprimer cette disponibilité">&times;</button>
		{% endif %}
	</div>
	<div class="gdhrdv-rdv-slot-content">
		<div class="gdhrdv-rdv-date-section">
			<label class="gdhrdv-rdv-date-label">
				<span class="dashicons dashicons-calendar"></span>
				<span>Choisissez une date</span>
				{% if isRequired %}
					<span class="gdhrdv-rdv-required">*</span>
				{% endif %}
			</label>
			<div class="gdhrdv-rdv-date-input-wrapper">
				<input type="date" name="slot_{{ index }}_date" class="gdhrdv-rdv-date" {% if isRequired %} required {% endif %} min="{{ todayStr }}">
				<span class="gdhrdv-rdv-date-icon dashicons dashicons-calendar-alt"></span>
			</div>
		</div>
		<div class="gdhrdv-rdv-time-section">
			<label class="gdhrdv-rdv-time-label">
				<span class="dashicons dashicons-clock"></span>
				<span>Sélectionnez vos horaires</span>
				{% if isRequired %}
					<span class="gdhrdv-rdv-required">*</span>
				{% endif %}
			</label>
			<div class="gdhrdv-rdv-times-grid">
				{% for slot in timeSlots %}
					<button type="button" class="gdhrdv-rdv-time" data-value="{{ slot.value }}" aria-pressed="false">
						<span class="gdhrdv-rdv-time-icon dashicons dashicons-clock"></span>
						<span class="gdhrdv-rdv-time-text">{{ slot.label }}</span>
						<span class="gdhrdv-rdv-time-check dashicons dashicons-yes"></span>
					</button>
				{% endfor %}
			</div>
			<div class="gdhrdv-rdv-all-day-section">
				<button type="button" class="gdhrdv-rdv-all-day" data-all-day aria-pressed="false">
					<span class="dashicons dashicons-clock"></span>
					<span>Toute la journée</span>
				</button>
			</div>
		</div>
	</div>
	<input type="hidden" name="slot_{{ index }}" class="gdhrdv-rdv-combined" {% if isRequired %} required {% endif %}>
</div>
